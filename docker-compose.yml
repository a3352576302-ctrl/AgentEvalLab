services:
  agentevallab:
    build: .
    volumes:
      # 挂载 reports/ 让报告能落到宿主机
      - ./reports:/app/reports
    # 如需真实 API 评测，请先 cp .env.example .env 并填入 Key，
    # 然后取消下面两行的注释：
    # env_file:
    #   - .env
